<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Contacto - Cuervo Bus</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />
    <style>
      body {
        font-family: "Inter", sans-serif;
        margin: 0;
        height: 100vh;
        background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)),
          url("https://i.ibb.co/gFZcxnK/background.jpg");
        background-size: cover;
        background-position: center;
        background-attachment: fixed;
        display: flex;
        flex-direction: column;
        margin: 0;
        font-family: "Inter", sans-serif;
        align-items: center;
        justify-content: center;
      }

      .blur-container {
        max-width: 1000px; /* Aumenta el ancho máximo */
        width: 90%; /* Ocupa el 90% del ancho de la pantalla, asegurando que sea responsivo */
        text-align: center;
        background-color: rgba(0, 0, 0, 0.8);
        padding: 60px; /* Aumenta el relleno para mayor espacio interno */
        border-radius: 10px;
        box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.5);
        backdrop-filter: blur(15px);
        background: rgba(216, 255, 212, 0.164);
        box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
        border-radius: 15px;
        border: 1px solid rgba(255, 255, 255, 0.18);
      }
      main {
        flex: 1;
      }
      footer {
        background: #1f2937; /* Gris oscuro */
        color: white;
        text-align: center;
        padding: 1rem 0;
        width: 100%; /* Asegura que ocupe todo el ancho */
        margin-top: auto;
      }

      footer a {
        color: #10b981; /* Verde */
        text-decoration: none;
        margin: 0 10px;
      }

      footer a:hover {
        color: #34d399; /* Verde más claro */
      }
      h1 {
        color: #52c41a;
        margin-bottom: 20px;
      }

      form {
        text-align: left;
      }

      label {
        color: #bfbfbf;
        display: block;
        margin-bottom: 5px;
      }

      input,
      textarea,
      button {
        width: 100%;
        padding: 10px;
        margin-bottom: 20px;
        border: none;
        border-radius: 5px;
        outline: none;
        font-size: 16px;
      }

      input,
      textarea {
        background-color: #333;
        color: #fff;
      }

      input:focus,
      textarea:focus {
        border: 2px solid #52c41a;
      }

      button {
        background-color: #52c41a;
        color: #fff;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }

      button:hover {
        background-color: #41a917;
      }

      .error {
        color: red;
        font-size: 14px;
        margin-top: -15px;
        margin-bottom: 15px;
      }
    </style>
  </head>

  <body class="flex flex-col justify-center items-center text-white">
    <main class="max-w-lg w-full p-8 text-center">
      <div class="max-w-lg w-full p-8 text-center blur-container">
        <h1>Contacto</h1>
        <% if (success) { %>
        <div style="color: green; margin-bottom: 20px">
          ¡Tu mensaje ha sido enviado con éxito!
        </div>
        <% } %>
        <form action="/contacto" method="POST">
          <label for="name">Nombre</label>
          <input type="text" id="name" name="name" required />

          <label for="email">Correo Electrónico</label>
          <input type="email" id="email" name="email" required />

          <label for="message">Mensaje</label>
          <textarea id="message" name="message" rows="5" required></textarea>

          <button type="submit">Enviar</button>
        </form>
      </div>
    </main>
    <%- include('footer') %>
  </body>
</html>
